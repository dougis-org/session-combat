## 1. UI Updates

- [x] 1.1 In `app/characters/[id]/page.tsx`, move the "Sync from D&D Beyond" button outside of the `!isEditing` condition so that it is visible on both the View and Edit screens.

## 2. Review and Verification

- [x] 2.1 Test that the sync button is visible when viewing a character.
- [x] 2.2 Test that the sync button is visible when editing a character.
- [x] 2.3 Verify that clicking the sync button while editing still correctly triggers the sync modal and the sync works properly.
