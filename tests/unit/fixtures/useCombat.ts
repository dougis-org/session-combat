import type { UseCombatReturn } from '@/lib/hooks/useCombat';
import type { CombatantState, CombatState } from '@/lib/types';

export function makeUseCombat(overrides?: Partial<UseCombatReturn>): UseCombatReturn {
  return {
    loading: false,
    error: null,
    encounters: [],
    characters: [],
    monsterTemplates: [],
    parties: [],
    combatState: null,
    setupCombatants: [],
    selectedEncounterId: '',
    selectedPartyId: null,
    initiativeMode: false,
    showCombatantModal: false,
    loadingTemplates: false,
    selectedDetailCombatantId: null,
    detailPosition: null,
    detailFocusSection: undefined,
    removeConfirmId: null,
    removeConfirmPosition: null,
    showEncounterDescription: false,
    toast: null,
    showLairForm: false,
    lairFormName: '',
    lairFormSeedMonster: '',

    setSelectedEncounterId: jest.fn(),
    selectParty: jest.fn(),
    setInitiativeMode: jest.fn(),
    setShowCombatantModal: jest.fn(),
    setSelectedDetailCombatantId: jest.fn(),
    setDetailPosition: jest.fn(),
    setDetailFocusSection: jest.fn(),
    setRemoveConfirmId: jest.fn(),
    setRemoveConfirmPosition: jest.fn(),
    setShowEncounterDescription: jest.fn(),
    showToast: jest.fn(),
    setShowLairForm: jest.fn(),
    setLairFormName: jest.fn(),
    setLairFormSeedMonster: jest.fn(),

    saveCombatState: jest.fn<Promise<void>, [CombatState | null]>().mockResolvedValue(undefined),
    addCombatantToSetup: jest.fn(),
    removeCombatantFromSetup: jest.fn(),
    cancelLairForm: jest.fn(),
    confirmAddLair: jest.fn(),
    addCombatantToActiveSession: jest.fn(),
    addCombatantFromLibrary: jest.fn(),
    startCombatWithSetupCombatants: jest.fn(),
    startCombat: jest.fn(),
    endCombat: jest.fn(),
    restartRound: jest.fn(),
    rollInitiative: jest.fn(),
    nextTurn: jest.fn(),
    updateCombatant: jest.fn(),
    updateCombatantInitiativeSettings: jest.fn(),
    removeCombatant: jest.fn(),
    setInitiativeRoll: jest.fn(),
    getDisplayCombatants: jest.fn<CombatantState[], []>().mockReturnValue([]),

    ...overrides,
  };
}
