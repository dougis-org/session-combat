name: Resolve outdated PR comments

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

jobs:
  resolve:
    uses: dougis-org/cicd-tooling/.github/workflows/resolve-outdated-comments.yml@main
