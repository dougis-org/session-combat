name: Sync openspec-shared submodule

on:
  push:
    branches: [main]

jobs:
  sync:
    permissions:
      contents: write
    uses: dougis-org/cicd-tooling/.github/workflows/sync-openspec-shared.yml@main
