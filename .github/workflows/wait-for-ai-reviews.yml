name: Wait for AI reviews

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

jobs:
  wait-for-ai-reviews:
    permissions:
      checks: read
      pull-requests: read
    uses: dougis-org/cicd-tooling/.github/workflows/wait-for-ai-reviews.yml@main
